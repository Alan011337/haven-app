# 📂 Document 3: Roadmap (The Execution Plan)

**檔案名稱：** `03_``[Roadmap.md](Roadmap.md)` 

**目標：** 在最短時間內，驗證「AI 生成卡牌能否改善伴侶關係」。

**最後更新：** 2026-02-03

## 🏁 Phase 1: Foundation (地基與記憶) 

- **任務**: 建立 Streamlit + Supabase 連線。

- **成果**: 解決了「失憶症」，系統現在擁有長期記憶能力。

## 🏗️ Phase 2: The Skeleton (骨架與核心資料) 

**目標：** 讓 App 能動起來，擁有基本的互動介面與資料讀寫能力。

- **Step 2.1: 資料庫模型建置 (Database Schema)**

   - 建立 `cards` 資料表 (存題目)。

   - 建立 `logs` 資料表 (存使用者的日記 input、情緒分析結果、卡牌回答)。

- **Step 2.2: 靜態卡牌與 UI 原型 (Static Loop)**

   - 手動寫入 5 張經典測試卡牌。

   - 實作 **Streamlit UI 佈局**：建立 `st.tabs` 分頁（"日記區" vs "抽卡區"）。

   - 實作「抽卡」按鈕與「回答輸入框」，確認能寫入 `logs`。

- **Step 2.3: AI 生成引擎 (The Generator)**

   - 建立 `src/``[cards.py](cards.py)`。

   - 串接 OpenAI API (`gpt-5-mini`)。

   - **關鍵任務**: 撰寫 "Framework Prompts" (把心理學理論寫進 Prompt)，讓 AI 能生成 JSON 格式的卡牌。

## 🧠 Phase 3: The Brain & Soul (情緒分析與遊戲化)

**目標：** 賦予 App 智慧，讓它能「讀懂」情緒並給予「獎勵」。

- **Step 3.1: 情緒翻譯機 (The Sentiment Engine)**

   - 開發 `src/``[sentiment.py](sentiment.py)`。

   - 實作 PRD 定義的 **4 維度 JSON 分析** (情緒、需求、行動、伴侶建議)。

   - 前端顯示：把冰冷的 JSON 轉化為溫暖的 UI 卡片。

- **Step 3.2: 智慧推薦邏輯 (The Matchmaker)**

   - 串接邏輯：日記情緒 -> AI 分析 -> 自動推薦適合的「卡牌模式」。

- **Step 3.3: 關係存款 (Feature C Implementation)**

   - 實作「完成按鈕」的回饋機制。

   - 在 `st.sidebar` 顯示累積的 "Meaningful Moments" 計數器。

## 🔐 Phase 4: The Gate (用戶系統與發布)

- **任務**: 實作簡單的 `st.sidebar` 登入/使用者切換。

- **目的**: 區分不同使用者的資料 (為 Alpha Test 做準備)。

- **最終任務**: Deploy to Streamlit Cloud。