name: Data Restore Drill

on:
  workflow_dispatch:
  schedule:
    - cron: "20 5 * * 1"

permissions:
  contents: read
  issues: write

jobs:
  data-restore-drill:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      DATABASE_URL: sqlite:///./data-restore-drill.db
      OPENAI_API_KEY: test-key
      SECRET_KEY: 01234567890123456789012345678901
      ABUSE_GUARD_STORE_BACKEND: memory
      DATA_SOFT_DELETE_ENABLED: "false"
      DATA_SOFT_DELETE_PURGE_RETENTION_DAYS: 90
      DATA_SOFT_DELETE_PURGE_EVIDENCE_MAX_AGE_DAYS: 14
      DATA_RESTORE_DRILL_EVIDENCE_MAX_AGE_DAYS: 35
      DATA_RESTORE_SOURCE_EVIDENCE_MAX_AGE_DAYS: 35
      DATA_RESTORE_DRILL_ENV: ci
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install backend deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Apply DB migrations
        run: alembic upgrade head

      - name: Run restore rehearsal drill
        id: drill
        continue-on-error: true
        run: |
          python scripts/run_data_soft_delete_purge_audit.py
          python scripts/validate_security_evidence.py --kind data-soft-delete-purge --contract-mode strict
          python scripts/run_data_restore_drill_audit.py
          python scripts/validate_security_evidence.py --kind data-restore-drill --contract-mode strict

      - name: Upload restore drill evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: data-restore-drill-evidence
          path: |
            docs/security/evidence/data-restore-drill-*
            docs/security/evidence/data-soft-delete-purge-*
          if-no-files-found: warn

      - name: Open or Update Alert Issue
        if: steps.drill.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const runUrl = `${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`;
            const titlePrefix = "[P0][Privacy] Data restore drill failed";
            const today = new Date().toISOString().slice(0, 10);
            const title = `${titlePrefix} (${today})`;
            const body = [
              "Data restore drill failed in CI.",
              "",
              `- Workflow run: ${runUrl}`,
              `- Ref: ${context.ref}`,
              `- SHA: ${context.sha}`,
              "",
              "Please inspect the uploaded `data-restore-drill-evidence` artifact and fix restore/deletion contract issues before next release."
            ].join("\n");

            const openIssues = await github.paginate(github.rest.issues.listForRepo, {
              owner,
              repo,
              state: "open",
              per_page: 100,
            });
            const existing = openIssues.find((issue) => issue.title.startsWith(titlePrefix));
            if (existing) {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: existing.number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner,
                repo,
                title,
                body,
              });
            }

      - name: Fail Job on Drill Failure
        if: steps.drill.outcome == 'failure'
        run: exit 1
