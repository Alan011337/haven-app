/**
 * Canonical import path for HTTP client.
 * Runtime implementation remains in `api.ts` to satisfy existing gate contracts.
 */
export { API_URL, api } from './api';
export { default } from './api';
