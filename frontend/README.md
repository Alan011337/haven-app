This is the Haven frontend (Next.js).

## Getting Started

Before startup, env vars are validated automatically (`predev`, `prebuild`, `prestart`).

Run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
